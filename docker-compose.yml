version: '3.8'

services:
  app-instafeed:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shopify-instafeed-app
    ports:
      - "127.0.0.1:7002:3000"

    env_file:
      - .env
    restart: unless-stopped
